#include "testlib.h"
#include <bits/stdc++.h>

using namespace std;

const long long MIN_N = 1, MAX_N = 679891637638612258;

int main() {
    registerValidation();
    long long n = inf.readLong(MIN_N,MAX_N);
    inf.readEoln();
    inf.readEof();
}